enum Sender { me, match }

enum ChatMessageType { text, contact, photo, video, voiceNote}

enum PlanType { wax, boom, free}

enum ErrorType { socket_exception, format_exception, http_exception, other_exception }

enum PhotoType {profile_photo}

enum VerificationStatus{ unverified, applicationSubmitted, verified}